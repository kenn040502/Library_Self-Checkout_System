# Postgres / Supabase database configuration
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Supabase configuration
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# DeepSeek (the only LLM provider)
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash
DEEPSEEK_API_BASE_URL=https://api.deepseek.com
DEEPSEEK_TIMEOUT_MS=15000
DEEPSEEK_STREAM_TIMEOUT_MS=30000

# OpenAI (optional fallback for book/auto-tag only — used when DeepSeek is unavailable)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# MCP recommendations (optional)
# Uses the local MCP server at mcp/server.mjs when enabled.
MCP_RECOMMENDATIONS_ENABLED=false
MCP_SERVER_COMMAND=
MCP_SERVER_ARGS=["mcp/server.mjs"]

# NextAuth Azure AD configuration
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
AZURE_AD_TENANT_ID=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# NextAuth defaults
AUTH_URL=http://localhost:3000/api/auth

# LinkedIn OAuth (Sign in with LinkedIn)
# Create an app at https://www.linkedin.com/developers/apps and enable "Sign In with LinkedIn using OpenID Connect"
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=

# YouTube Data API v3 integration (Learning Hub)
# Get an API key from https://console.cloud.google.com → APIs & Services → Credentials
# Enable "YouTube Data API v3" in Library
YOUTUBE_API_KEY=

# Optional: customize landing page "Trending" feed (videos.list chart=mostPopular)
# Region code examples: MY, US, SG
YOUTUBE_TRENDING_REGION_CODE=MY
# Category id: 28 = Science & Technology (default). Note: some categories (e.g. 27 Education) may return 404 for chart=mostPopular.
YOUTUBE_TRENDING_CATEGORY_ID=28

# SIP2 emulator configuration
SIP2_BASE_URL=
SIP2_API_KEY=

# Development testing (optional)
DEV_AZURE_EMAIL_SUFFIX=

